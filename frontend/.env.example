# Copy to .env.local for local dev (usually not needed — the Vite proxy handles /api).
# In production (Vercel), set VITE_API_BASE to your deployed Render backend origin:
#   VITE_API_BASE=https://ai-cam-recognize-backend.onrender.com
VITE_API_BASE=
